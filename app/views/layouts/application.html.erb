<!DOCTYPE html>
<html>
  <head>
    <title>WAI_to_WAI</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- ① アプリ固有 SCSS（Sprockets 側） -->
    <%= stylesheet_link_tag 'application',
          media: 'all',
          'data-turbolinks-track': 'reload' %>

    <!-- ② Bootstrap 4.6.2 CSS -->
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/bootstrap@4.6.2/dist/css/bootstrap.min.css">

    <!-- ③ Webpacker バンドル（application.js のみで全機能を呼ぶ） -->
    <%= javascript_pack_tag 'application',
          'data-turbolinks-track': 'reload' %>

    <!-- ④ Font Awesome Kit -->
    <script src="https://kit.fontawesome.com/e1acaa1ac8.js"
            crossorigin="anonymous"></script>

    <!-- ⑤ Google Maps（callback なし／async+defer）-->
    <script src="https://maps.googleapis.com/maps/api/js?key=<%= ENV['GOOGLE_MAPS_API_KEY'] %>&loading=async&libraries=marker" async defer></script>

  </head>

  <body class="d-flex flex-column vh-100" style="background-color:#F8F9FA;">
    <!-- ===== ヘッダー ===== -->
    <%= render 'layouts/user_header' %>

    <!-- ===== メイン ===== -->
    <main class="mb-auto">
      <% if flash[:notice].present? %>
        <p class="text-success px-3"><%= flash[:notice] %></p>
      <% end %>
      <% if flash[:alert].present? %>
        <p class="text-danger  px-3"><%= flash[:alert]  %></p>
      <% end %>

      <%= yield %>
    </main>

    <!-- ===== フッター ===== -->
    <%= render 'layouts/footer' %>
  </body>
</html>