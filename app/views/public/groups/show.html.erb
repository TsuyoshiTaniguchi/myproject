<h2>所属メンバー一覧</h2>

<% if @group.users.any? %>
  <ul>
    <% @group.users.each do |user| %>
      <li><%= link_to user.name, user_path(user) %></li>
    <% end %>
  </ul>
<% else %>
  <p>このグループにはまだメンバーがいません。</p>
<% end %>

<% if @group.users.include?(current_user) %>
  <%= button_to "グループを脱退する", group_membership_path(@group, @membership), method: :delete, class: "btn btn-danger" %>
<% else %>
  <%= button_to "このグループに参加する", group_memberships_path(@group), method: :post, class: "btn btn-primary" %>
<% end %>