<h1><%= @user.name %> さんの所属グループ</h1>

<% if @groups.any? %>
  <ul>
    <% @groups.each do |group| %>
      <li><%= link_to group.name, user_group_path(@user, group) %></li>
    <% end %>
  </ul>
<% else %>
  <p>このユーザーはまだグループに参加していません。</p>
<% end %>

<%= link_to "ユーザー一覧へ戻る", users_path, class: "btn btn-outline-secondary mt-3" %>