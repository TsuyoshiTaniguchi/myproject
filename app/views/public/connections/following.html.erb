<h1 class="mb-4 text-center font-weight-bold"><%= @user.name %> さんのフォローしているユーザー一覧</h1>

<div id="following-list" class="mb-4">
  <ul class="list-group">
    <% if @users.any? %>
      <% @users.each do |user| %>
        <li class="list-group-item">
          <%= link_to user.name, user_path(user), class: "text-decoration-none" %>
        </li>
      <% end %>
    <% else %>
      <li class="list-group-item text-center text-muted">
        まだフォローしているユーザーがいません。
      </li>
    <% end %>
  </ul>
</div>

<div class="d-flex justify-content-center">
  <%= link_to "マイページに戻る", user_path(current_user), class: "btn btn-secondary me-2" %>
  <%= link_to "ユーザー詳細に戻る", user_path(@user), class: "btn btn-secondary" %>
</div>